import { MainnetSetup } from "@/components/launchpad/setup";
export const metadata = {
  title: "Mainnet setup",
  robots: { index: false, follow: false },
};
export default function Page() {
  return <MainnetSetup />;
}
